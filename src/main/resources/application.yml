server:
  port: 8081

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/gallery_db
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  storage:
    upload-dir: ./uploads
  base-url: http://localhost:8081
  cors-origin: http://localhost:5173
